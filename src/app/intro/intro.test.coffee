'use strict'
describe 'controllers', ->
  scope = undefined
  beforeEach module('ngCardboard')
  beforeEach inject(($rootScope) ->
    scope = $rootScope.$new()
    return
  )
  
  #place your tests here
  
  return
